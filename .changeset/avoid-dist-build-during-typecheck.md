---
"effect-solutions": patch
"effect-solutions-mcp": patch
---

Stop `tsc --build` from emitting dist test artifacts so `bun test` only runs source tests; also tidy CLI open-issue logging and update notifier typings.
